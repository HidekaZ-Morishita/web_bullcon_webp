import { renderForm } from './form_ui.js'; import { setupEventListeners } from './event_handler.js'; import { exportTableToPdf, generatePdfPreviewUrl } from './result_table_exporter.js'; import { getCompatibilityData } from './match_api_client.js'; import { loadProductUrlMap } from './product_url_loader.js'; const CAR_MODEL_API = '../../api/web_page/get_car_model.php'; const MONITOR_NUMBER_LIST_API = '../../api/web_page/get_monitor_list.php'; const NOTE_LIST_API = '../../api/web_page/get_note_list.php'; let carModelCache = null; let monitorNumberCache = null; let noteCash = null; document.addEventListener('DOMContentLoaded', async() => { await initializeAndGetCarModel(); await initializeAndGetMonitorNumber(); await initializeAndGetMapNotes(); renderForm('form-container', { selectedProduct: null, selectedOptionType: null, selectedInputType: null, selectedMaker: null, selectedModel: null, selectedYear: null, selectedMonth: null, selectedProductCode: null, }); setupEventListeners(); const exportButton    = document.getElementById('export-pdf-button'); const pdfDialog       = document.getElementById('pdf-export-dialog'); const pdfDialogCancel = document.getElementById('pdf-dialog-cancel'); const pdfDialogPreview = document.getElementById('pdf-dialog-preview'); const pdfDialogSave   = document.getElementById('pdf-dialog-save'); const pdfPreviewFrame = document.getElementById('pdf-preview-frame'); const pdfPreviewLoading = document.getElementById('pdf-preview-loading'); let currentPreviewUrl = null; const closePdfDialog = () => { pdfDialog.close(); pdfDialog.classList.remove('preview-active'); pdfPreviewFrame.src = ''; _removeStaleOverlay(); if (currentPreviewUrl) { URL.revokeObjectURL(currentPreviewUrl); currentPreviewUrl = null; } }; const _showStaleOverlay = () => { const wrapper = document.getElementById('pdf-preview-frame-wrapper'); if (!wrapper || wrapper.querySelector('.pdf-preview-stale')) return; const overlay = document.createElement('div'); overlay.className = 'pdf-preview-stale'; overlay.innerHTML = '<span class="pdf-preview-stale-icon">&#8635;</span><span>設定が変更されました。<br>「プレビュー」ボタンで更新してください。</span>'; wrapper.appendChild(overlay); }; const _removeStaleOverlay = () => { const wrapper = document.getElementById('pdf-preview-frame-wrapper'); wrapper?.querySelector('.pdf-preview-stale')?.remove(); }; if (exportButton && pdfDialog) { exportButton.addEventListener('click', () => { pdfDialog.showModal(); }); pdfDialogCancel.addEventListener('click', closePdfDialog); pdfDialog.addEventListener('close', () => { pdfDialog.classList.remove('preview-active'); pdfPreviewFrame.src = ''; _removeStaleOverlay(); if (currentPreviewUrl) { URL.revokeObjectURL(currentPreviewUrl); currentPreviewUrl = null; } }); pdfDialog.addEventListener('change', () => { if (pdfDialog.classList.contains('preview-active')) { _showStaleOverlay(); } }); pdfDialogPreview.addEventListener('click', async () => { const orientation = document.querySelector('input[name="pdf-orientation"]:checked')?.value || 'landscape'; const format = document.getElementById('pdf-format-select')?.value || 'a4'; pdfDialog.classList.add('preview-active'); pdfPreviewFrame.src = ''; _removeStaleOverlay(); pdfPreviewLoading.style.display = 'flex'; pdfDialogPreview.disabled = true; if (currentPreviewUrl) { URL.revokeObjectURL(currentPreviewUrl); currentPreviewUrl = null; } try { const blobUrl = await generatePdfPreviewUrl('result', { orientation, format }); currentPreviewUrl = blobUrl; pdfPreviewFrame.src = blobUrl; } catch (error) { console.error('プレビュー生成エラー:', error); } finally { pdfPreviewLoading.style.display = 'none'; pdfDialogPreview.disabled = false; } }); pdfDialogSave.addEventListener('click', async () => { const orientation = document.querySelector('input[name="pdf-orientation"]:checked')?.value || 'landscape'; const format = document.getElementById('pdf-format-select')?.value || 'a4'; const rawFilename = document.getElementById('pdf-filename-input')?.value.trim() || 'compatibility_result'; closePdfDialog(); pdfDialogSave.disabled = true; await exportTableToPdf('result', { orientation, format, filename: rawFilename }); pdfDialogSave.disabled = false; }); } }); const initializeAndGetCarModel = async() => { if (carModelCache) { return carModelCache; } const carModel = await getCompatibilityData(CAR_MODEL_API , ""); carModelCache = carModel; return carModel; }; const initializeAndGetMonitorNumber = async() => { if (monitorNumberCache) { return monitorNumberCache; } const monitorNumberList = await getCompatibilityData(MONITOR_NUMBER_LIST_API , ""); monitorNumberCache = monitorNumberList; return monitorNumberList; }; const initializeAndGetMapNotes = async() => { if (noteCash) { return noteCash; } const noteList = await getCompatibilityData(NOTE_LIST_API , ""); const notesMap = { 'televing': noteList.notes_tving || [], 'magicone_bk_un': noteList.notes_back_camera || [], 'magicone_bk_ha': noteList.notes_back_camera || [], 'magicone_rm_un': noteList.notes_rearmonitor_vtr_hdmi || [], 'magicone_rm_ha': noteList.notes_rearmonitor_vtr_hdmi || [], 'magicone_vtr_hdmi': noteList.notes_rearmonitor_vtr_hdmi || [], 'camera_selector': noteList.notes_camera_selector || [], 'steering_swt_ctrl': noteList.notes_steering_switch_controller || [], 'dvd_cd_player': noteList.notes_dvd_player || []
}; noteCash = notesMap; return notesMap; }; export { initializeAndGetCarModel, initializeAndGetMonitorNumber, initializeAndGetMapNotes
};