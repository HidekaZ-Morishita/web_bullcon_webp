function _getHtml2Canvas() { return window.html2canvas || null; } function _getJsPdf() { if (window.jspdf && window.jspdf.jsPDF) { return window.jspdf.jsPDF; } if (window.jsPDF) { return window.jsPDF; } return null; } const CAPTURE_DELAY_MS = 100; const PDF_COMPRESSION_STYLE = `
/* ラッパー全体にパディングを適用し、PDF端からの余白を確保 */
#pdf-capture-wrapper { padding: 10px !important; } /* テーブル全体の文字サイズを緩和 */
#pdf-capture-wrapper, #pdf-capture-wrapper table, #pdf-capture-wrapper p, #pdf-capture-wrapper li { font-size: 10pt !important; line-height: 1.3 !important; } /* セル（th, td）のパディングを適度に削減 */
#pdf-capture-wrapper th, #pdf-capture-wrapper td { padding: 4px 6px !important; } /* テーブルのボーダー間隔を詰める */
#pdf-capture-wrapper table { border-collapse: collapse !important; } /* 注意事項のリストの余白を詰める */
#pdf-capture-wrapper ul, #pdf-capture-wrapper ol { padding-left: 15px !important; margin-top: 5px !important; margin-bottom: 5px !important; } `; async function _captureTableToCanvas(tableContainerId) { const html2canvasLib = _getHtml2Canvas(); const jsPdfLib = _getJsPdf(); if (!html2canvasLib || !jsPdfLib) { throw new Error('PDF出力ライブラリがロードされていません。ライブラリのロードを確認してください。'); } const reverseWrapper = document.getElementById('reverse-results-wrapper'); const standardTableContainer = document.getElementById('results-table-container'); const standardTable = standardTableContainer ? standardTableContainer.querySelector('.result-table') : null; let targetElementToCapture = null; let isReverseMode = false; if (reverseWrapper && reverseWrapper.children.length > 0) { targetElementToCapture = reverseWrapper; isReverseMode = true; } else if (standardTableContainer && standardTable && standardTableContainer.style.display !== 'none') { targetElementToCapture = standardTableContainer; } else { const fallbackContainer = document.getElementById(tableContainerId) || document.getElementById('result'); if (fallbackContainer) { targetElementToCapture = fallbackContainer; } } if (!targetElementToCapture) { throw new Error('PDF出力対象の適合結果テーブルが見つかりません。'); } const tableHeaders = targetElementToCapture.querySelectorAll('thead'); const originalBodyOverflowX = document.body.style.overflowX; document.body.style.overflowX = 'visible'; tableHeaders.forEach(th => { th.style.position = 'static'; }); const tempWrapper = document.createElement('div'); tempWrapper.id = 'pdf-capture-wrapper'; const styleElement = document.createElement('style'); styleElement.textContent = PDF_COMPRESSION_STYLE; tempWrapper.appendChild(styleElement); tempWrapper.appendChild(targetElementToCapture.cloneNode(true)); const originalNotesContainer = document.getElementById('notes-list-container'); if (!isReverseMode && originalNotesContainer && originalNotesContainer.style.display !== 'none') { tempWrapper.appendChild(originalNotesContainer.cloneNode(true)); } Object.assign(tempWrapper.style, { position: 'absolute', left: '-9999px', top: '0', zIndex: '9999', overflow: 'visible', maxWidth: 'none', maxHeight: 'none', width: 'fit-content', }); document.body.appendChild(tempWrapper); await new Promise(resolve => setTimeout(resolve, CAPTURE_DELAY_MS)); const captureWidth = tempWrapper.offsetWidth; const captureHeight = tempWrapper.scrollHeight; tempWrapper.style.width = `${captureWidth}px`; const canvas = await html2canvasLib(tempWrapper, { scale: 2, useCORS: true, allowTaint: true, letterRendering: true, width: captureWidth, height: captureHeight, }); const cleanup = () => { document.body.style.overflowX = originalBodyOverflowX; if (tempWrapper.parentNode) { tempWrapper.parentNode.removeChild(tempWrapper); } }; return { canvas, cleanup }; } function _buildPdfFromCanvas(canvas, { orientation = 'landscape', format = 'a4' } = {}) { const jsPdfLib = _getJsPdf(); if (!jsPdfLib) { throw new Error('PDF出力ライブラリ(jsPDF)が見つかりません。'); } const pdf = new jsPdfLib({ orientation, unit: 'mm', format }); const pdfWidth = pdf.internal.pageSize.getWidth(); const pdfHeight = pdf.internal.pageSize.getHeight(); const imgData = canvas.toDataURL('image/jpeg', 0.98); const ratio = canvas.height / canvas.width; const imgWidth = pdfWidth; const imgHeight = imgWidth * ratio; let heightLeft = imgHeight; let position = 0; while (heightLeft > 0) { if (position !== 0) pdf.addPage(); pdf.addImage(imgData, 'JPEG', 0, -position, imgWidth, imgHeight); heightLeft -= pdfHeight; position += pdfHeight; } return pdf; } export async function exportTableToPdf(tableContainerId, options = {}) { const { orientation = 'landscape', format = 'a4', filename = 'compatibility_result', } = options; const exportButton = document.getElementById('export-pdf-button'); if (exportButton) exportButton.disabled = true; let cleanup = null; try { const { canvas, cleanup: _cleanup } = await _captureTableToCanvas(tableContainerId); cleanup = _cleanup; const pdf = _buildPdfFromCanvas(canvas, { orientation, format }); pdf.save(`${filename}.pdf`); } catch (error) { console.error('PDF生成中にエラーが発生しました:', error); } finally { if (cleanup) cleanup(); if (exportButton) exportButton.disabled = false; } } export async function generatePdfPreviewUrl(tableContainerId, options = {}) { const { orientation = 'landscape', format = 'a4' } = options; const { canvas, cleanup } = await _captureTableToCanvas(tableContainerId); try { const pdf = _buildPdfFromCanvas(canvas, { orientation, format }); return pdf.output('bloburl'); } finally { cleanup(); } }